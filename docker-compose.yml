version: '3.8'

services:
  # Base de données MySQL
  mysql:
    image: mysql:8.0
    container_name: toumai_drive_mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: toumai_drive
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - toumai_net

  # API Backend Laravel
  backend:
    image: php:8.2-cli
    container_name: toumai_drive_backend
    restart: unless-stopped
    working_dir: /var/www/backend
    volumes:
      - ./backend-vehicules:/var/www/backend
    ports:
      - "8000:8000"
    command: bash -c "docker-php-ext-install pdo pdo_mysql && php artisan serve --host=0.0.0.0 --port=8000"
    depends_on:
      - mysql
    networks:
      - toumai_net

  # Frontend Next.js
  frontend:
    image: node:18-alpine
    container_name: toumai_drive_frontend
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./frontend-vehicules:/app
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm run dev"
    depends_on:
      - backend
    networks:
      - toumai_net

volumes:
  mysql_data:

networks:
  toumai_net:
    driver: bridge
